<%= form_for(@mcategory) do |f| %>
  <% if @mcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mcategory.errors.count, "error") %> prohibited this mcategory from being saved:</h2>

      <ul>
      <% @mcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
