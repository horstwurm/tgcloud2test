
  <div class="row">
    <header>
    <%= header((I18n.t :tabellenmgt), true) %>
    </header>
  </div>

	<div class="row">
    <div class="col-md-12">
      <div class="panel panel-default">
    		<!--<table class="table table-striped table-hover-->
    		<table class="table table-striped">
          <thead>
            <tr>
              <th><%= I18n.t :kategorie %></th>
            </tr>
          </thead>
        
          <tbody>
            <% @categories.each do |c| %>
              <tr><td>
              <%= link_to mcategories_path :ctype => c.ctype do %>
                <i class="glyphicon glyphicon-folder-open"></i>
              <% end %>
              <%= c.ctype %>
              </td></tr>
            <% end %>
          </tbody>

        </table>
      </div>
    </div>
  </div>
